# v0.4 implementation plan: candidate generation

## Outcome

Enumerate every source-record pair admitted by the definition's exact, composite, prefix, and token predicates. Canonicalize and deduplicate pairs across channels. Reject an oversized block or aggregate candidate set before returning any result.

The release proves both the Rust path and production-generated PostgreSQL SQL against an independent all-pairs oracle on generated small datasets. It does not run an all-pairs query in production and does not execute a live `pg_trickle` graph.

## Prerequisites and dependency changes

Start from v0.3 with normalized `value` rows and stable source-record sort keys. Candidate code must ignore every non-value state.

Add `proptest` as a development dependency. Add no runtime crate. Use `BTreeMap` and `BTreeSet` for deterministic grouping, ordering, and deduplication. A plain digest of a low-entropy block key is sensitive. Keep it only in protected internal diagnostics and omit it from public errors and ordinary logs.

Add the 0.3.0 to 0.4.0 extension migration and archive the generated 0.4.0 install SQL. The migration may contain only function replacements and version metadata if no durable table changes are needed.

## File layout

Add or change these files:

```text
src/
  candidate.rs            channel evaluation, block checks, and pair union
  candidate/
    exact.rs              exact and composite block keys
    prefix.rs             deterministic Unicode-scalar prefixes
    token.rs              sorted unique token blocks
    oracle.rs             independent test-only all-pairs reference
  definition/
    validate.rs           channel and limit validation
  graph_spec.rs           block and canonical-pair stage SQL
  error.rs                candidate completeness result codes
  semantics.rs            versioned default and absolute limits
sql/
  pg_mdm--0.3.0--0.4.0.sql
  archive/pg_mdm--0.4.0.sql
tests/
  fixtures/candidate_cases.json
  fixtures/organization_domain_v1.json
  candidate_tests.rs
  candidate_property_tests.rs
  e2e_candidate_spec_tests.rs
  e2e_upgrade_tests.rs
```

Keep `oracle.rs` behind `cfg(test)`. Do not expose an all-pairs helper in the packaged extension.

## Data and schema skeleton

Do not add a durable production table. Model the future derived relation with Rust records and temporary test tables:

```rust
pub(crate) struct NormalizedRecord {
	pub source_record_id: pgrx::Uuid,
	pub source_sort_key: Vec<u8>,
	pub field: String,
	pub normalized: String,
	pub canonical_bytes: Vec<u8>,
}

pub(crate) struct CandidatePair {
	pub left_source_record_id: pgrx::Uuid,
	pub right_source_record_id: pgrx::Uuid,
	pub left_sort_key: Vec<u8>,
	pub right_sort_key: Vec<u8>,
	pub discovery_channels: Vec<String>,
}

pub(crate) struct CandidateLimits {
	pub max_block_records: usize,
	pub max_candidate_pairs: usize,
}
```

Freeze `candidate_blocks_v1` and `candidate_pairs_v1` as terminal-relation contracts in the compiled artifact. They have these logical row shapes:

```text
candidate block:
  channel_id, block_key, source_record_id, source_sort_key

canonical candidate pair:
  left_source_record_id, right_source_record_id,
  left_sort_key, right_sort_key, discovery_channels
```

Order each pair by `source_sort_key`, not by UUID. Require different source-record IDs and emit one row per logical pair. Sort `discovery_channels` by stable channel ID.

## Candidate channel contract

Compile these V1 channels:

| Channel | Block membership |
|---|---|
| `exact` | One normalized field has identical canonical bytes |
| `composite_exact` | Every named field is `value`, and the ordered tuple of canonical bytes is identical |
| `prefix` | The first configured number of Unicode scalar values is identical after normalization |
| `token` | At least one configured token from the normalized value is identical |

The token channel splits on normalized whitespace, rejects tokens below the configured minimum scalar length, removes duplicate tokens within one record, and sorts the rest by UTF-8 bytes. It does not stem words, remove language-specific stop words, or use approximate retrieval.

An identity or strong match may own a channel. A supporting match may be evaluated for an already discovered pair but cannot own a channel. A fuzzy match must name at least one complete channel unless another identity or strong rule already discovers the same needed pairs through explicit compiled lineage.

The expanded definition stores `max_block_records`, `max_candidate_pairs`, and `warning_block_records`. Measure positive defaults in `src/semantics.rs` before release, record them in the semantic manifest, and document the tested operating envelope. Follow v0.2's rule for missing historical semantic inputs: a new default or changed limit requires an explicit new definition version. An artifact cannot supply it silently. A definition may lower a limit. Raising a limit above the compiled absolute ceiling requires an extension upgrade and a new definition version.

## Completeness algorithm

For each channel:

1. Build the full block key for every eligible normalized record.
2. Group records by the full key. Never group by a digest alone.
3. Sort members by `source_sort_key` and reject duplicate sort keys.
4. In a separate block-statistics stage, reject a block over `max_block_records` with `MDM_CANDIDATE_BLOCK_LIMIT`, the channel, cardinality, configured limit, and next action. Put any block key or SHA-256 digest only in protected internal diagnostics.
5. Compute `n * (n - 1) / 2` with checked arithmetic before allocating pairs.
6. Enumerate all pairs in the accepted block and insert them into one ordered set.
7. If the ordered set would exceed `max_candidate_pairs`, return `MDM_CANDIDATE_TOTAL_LIMIT` and discard the in-memory result.
8. Add the channel ID to an existing pair when another channel discovers it.

The pair stage depends on the successful block-statistics stage, so an oversized block fails before PostgreSQL executes its self-join. Because callers receive a result only after all channels succeed, an error cannot expose a partial candidate set. The later refresh transaction also rolls back any derived work. The aggregate unique-pair limit necessarily follows deduplication; measure its temporary storage and memory rather than claiming pre-allocation rejection.

Prove this ordering with an instrumented pair stage under changed query plans. An unused validation CTE or a planner-reorderable expression does not establish the barrier. Repeat the same case through strict graph refresh at v0.8 admission; the ordinary SQL test cannot prove upstream graph scheduling.

## API skeleton

Add no public SQL function. Extend the internal Rust API:

```rust
pub(crate) fn generate_candidates(
	plan: &CandidatePlan,
	values: &[NormalizedRecord],
	limits: CandidateLimits,
) -> Result<Vec<CandidatePair>, MdmError>;

#[cfg(test)]
pub(crate) fn all_pairs_oracle(
	plan: &CandidatePlan,
	values: &[NormalizedRecord],
) -> Vec<CandidatePair>;
```

The oracle evaluates each unordered record pair directly against the declarative channel predicates. It does not call block-key builders, channel evaluators, pair-union code, production serializers, or generated SQL helpers.

Under the `pg_test` feature, expose a JSONB fixture wrapper for end-to-end limit and SQL-stage tests. Omit the wrapper and the all-pairs oracle from release packages.

Extend `mdm.describe()` with the logical channel ID, channel kind, fields, parameters, owning rules, warning threshold, hard block limit, and aggregate pair limit. Do not expose generated private relation names.

The graph-spec compiler emits ordinary relational SQL for block membership, separate block-limit validation, and pair union. Each equality stage retains the full block value. Generated SQL follows the v0.2 allowlist and becomes an immutable artifact attachment. Execute it against rollback-only PostgreSQL fixtures under the selected execution role, row-level security, and a hostile `search_path`. v0.8 decides physical indexes after `pg_trickle` accepts the graph.

## Implementation order

1. Freeze the channel JSON format, stable channel IDs, and limit fields.
2. Implement canonical pair keys and checked pair-count arithmetic.
3. Implement exact and composite channels.
4. Implement prefix and token channels.
5. Add cross-channel union, deduplication, and completeness errors.
6. Write the independent test-only all-pairs oracle and generated comparisons.
7. Emit deterministic block-limit and pair stages in `graph_spec.rs`, then execute them against fixture sources.
8. Add the labeled organization corpus and measure the provisional pilot envelope.
9. Extend `describe`, migrations, upgrades, and the operating-envelope report.

## Test matrix

| Level | Case | Expected result |
|---|---|---|
| Unit | Exact values with null and non-value states | Only equal `value` rows share a block |
| Unit | Composite channel with one absent member | The record joins no composite block |
| Unit | Prefix length at zero, at value length, and above value length | Validation rejects zero; the other cases are deterministic |
| Unit | Repeated tokens within one record | No self-pair or duplicate pair is emitted |
| Unit | One pair found by several channels | One pair contains sorted unique channel IDs |
| Unit | UUID order differs from source sort order | Left and right follow source sort order |
| Unit | Block at and one over the hard limit | The first completes; the second returns `MDM_CANDIDATE_BLOCK_LIMIT` |
| Unit | Aggregate pair count at and one over its limit | The first completes; the second returns `MDM_CANDIDATE_TOTAL_LIMIT` |
| Unit | Pair-count arithmetic near `usize` overflow | Checked arithmetic returns a limit error without allocation |
| Property | Random small records and channel plans | Generated candidates equal the all-pairs oracle |
| Property | Run the same generated cases through PostgreSQL stage SQL | SQL rows equal the independent oracle without using production helper results as expectations |
| Property | Shuffle records, fields, and channel evaluation order | The result bytes remain identical |
| Property | Add duplicate discovery channels | Pair membership stays unchanged |
| End to end | Execute one channel of every kind against fixture source tables | Production terminal readers return exactly the independent expected rows |
| End to end | Cause a block overflow in a temporary fixture relation | The limit stage fails before the pair join runs; no candidate result table or operation success row remains |
| End to end | Use malicious identifier-shaped names and unsupported object references | Generated SQL remains one qualified allowlisted query or compilation fails |
| Security | Execute generated SQL under row-level security, revoked access, hostile `search_path`, and an injected error | Visibility, fail-closed behavior, and context restoration match the execution-role contract |
| Quality | Run the labeled organization corpus | Candidate recall and missed-match slices are recorded before pair thresholds are chosen |
| Resource | Sweep source rows, block skew, channel overlap, and pair limits | Report block memberships, repeated discoveries, unique pairs, memory, temporary bytes, time, and failure boundary |
| Upgrade | Upgrade every prior archived release | Definitions, source identities, and definition digests remain unchanged; the new artifact attaches append-only |

Add these commands:

```text
just test-candidates
just test-candidate-properties
```

## Exit criteria

- [ ] Every V1 channel has a frozen JSON contract, version, and fixture case.
- [ ] Both generated Rust results and production-generated SQL match the independent all-pairs oracle.
- [ ] Input order, plan order, UUID order, and PostgreSQL collation do not change candidate bytes.
- [ ] A hard block or aggregate limit returns a stable error and no partial result.
- [ ] Supporting evidence cannot create a candidate channel.
- [ ] `mdm.describe()` reports the complete logical plan and all warning and hard limits.
- [ ] PostgreSQL executes every generated stage under the production execution-role contract, and the production terminal reader returns the expected rows.
- [ ] The measured defaults and absolute ceilings are positive, documented, and included in the semantic manifest.
- [ ] The provisional pilot envelope records source rows, block skew, repeated and unique pairs, peak memory, temporary bytes, duration, and the fixture and environment digests.
- [ ] The labeled organization corpus records candidate recall and missed-match risk slices for the proposed channels.
- [ ] Upgrade tests and the complete v0.1 through v0.3 matrices pass.

## Deferred

Do not add phonetic indexes, trigram indexes, locality-sensitive hashing, nearest-neighbor search, adaptive truncation, or plan-dependent sampling. Add a new channel only when its complete logical predicate and hard failure behavior are specified.

After this release, re-estimate v0.5 through v0.7 from measured candidate growth and PostgreSQL resource use.
