# v0.11 implementation plan: release qualification

## Boundary

Keep the v0.10 SQL API and PostgreSQL behavior. Add release qualification for
the exact `pg_trickle` 0.105.1 package already pinned in `DEPENDENCIES.md` and
CI. Do not add a new upstream contract or MDM feature.

## Qualification suite

- [x] Refresh matching Graph V1 probe tables in `AUTO` and `FULL` modes together.
  Compare their rows at each returned source boundary after inserts, updates,
  deletes, and no-op refreshes. Require `AUTO` to report differential
  maintenance on the supported steady-state scan, and require `FULL` to report
  full maintenance. (The tagged v0.11 run passed these earlier assertions.)
- [ ] Drive the compiled MDM graph through source inserts, updates, deletes, and
  no-op refreshes. Compare active memberships with the independent fixture
  expectations for duplicate-email merges and splits. (The tagged v0.11 run
  fails at `tests/e2e.sql:1022`: duplicate-email records are not merged.)
- [ ] Inject an output-write failure after Graph V1 maintenance. Check that the
  publication revision, history, and public entity, member, and review rows
  remain unchanged. Retry and require the source change to appear at a proven
  source boundary. (Not reached because the tagged v0.11 E2E run stops earlier.)
- [ ] Commit a source write after graph maintenance has returned its boundary but
  before MDM publication. Require that write to remain pending until the next
  refresh. (Not reached because the tagged v0.11 E2E run stops earlier.)
- [ ] Keep the cumulative capability, authorization, RLS, concurrency, resolver
  limit, upgrade, backup, restore, and clone checks in the same CI suite.
  (The tagged v0.11 E2E suite aborts on the duplicate-email merge failure.)

The paired Graph V1 fixtures use only public `pg_trickle` SQL. The MDM tests
read public outputs and do not inspect `pg_trickle` catalogs, change buffers,
or scheduler state.

## Release packaging

- [x] Set the extension package version to `0.11.0`.
- [x] Add the `0.10.0` to `0.11.0` update path without changing runtime objects.
- [x] Archive the generated `pg_mdm--0.11.0.sql` installation script.
- [x] Test sequential upgrades from `0.1.0` and direct upgrades from `0.2.0`. (The tagged E2E run passed both upgrade starts before reaching the later failing assertion.)
- [x] Keep the PostgreSQL 18 image and `pg_trickle` artifact pins unchanged. (Tagged v0.11 E2E used the pinned image and 0.105.1 checksum.)

## Exit evidence

- [x] Formatting, Clippy, library/pgrx tests, security and upgrade checks, and package/archive comparison pass in the tagged v0.11 tester image.
- [ ] The Docker E2E suite passes against the pinned PostgreSQL 18 image and `pg_trickle` 0.105.1 checksum. (It fails at the duplicate-email merge assertion noted above.)
- [x] CI generates and uploads a package manifest with each package file's byte count and SHA-256. (The tagged v0.11 manifest was generated locally and contains those fields.)

- [x] The README includes v0.11 install, refresh, and restore instructions.
- [ ] The `v0.11.0` tag is ready only after every CI job passes. (The tag exists, but the tagged E2E rerun fails.)
